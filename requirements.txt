# Core dependencies for Latent World Model (CSCI 490)
# Tested with Python 3.10+. CPU-only PyTorch is sufficient — no GPU required.

numpy>=1.24,<3
torch>=2.0
pandas>=2.0
matplotlib>=3.7

# Optional — only Phase 1-3/gravity_model.py uses this (LLM probe via Ollama).
# Uncomment if you want to run that script.
# ollama>=0.1
